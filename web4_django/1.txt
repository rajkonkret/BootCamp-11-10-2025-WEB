pip install Django==5.2.5 - instalacja wskazanej wersji Django
python -m django --version - sprawdzenie wersji
django-admin --version

django-admin startproject myproject - tworzenie projektu django

cd myproject
python manage.py runserver - uruchomienie projektu djangowego
gunicorn, uwsgi, uvicorn
gunicorn -b :5000 main:app - uruchommienie na porcie 5000

├── manage.py ├── myproject/ # Główny plik zarządzania projektem
# Folder aplikacji głównej
│ ├── __init__.py
│ ├── settings.py # Główna konfiguracja Django
│ ├── urls.py # Routing URL
│ ├── asgi.py
│ ├── wsgi.py

python manage.py startapp myapp - utworzenie apliakcji django

python manage.py migrate - migracja bazy

http://127.0.0.1:8000/admin/ - panel admina
python manage.py createsuperuser - tworzenie admina
(.venv) radoslawjaniak@mac myproject % python manage.py createsuperuser
Username (leave blank to use 'radoslawjaniak'): admin
Email address: admin@admin.pl
Password:
Password (again):
This password is too short. It must contain at least 8 characters.
This password is too common.
Bypass password validation and create user anyway? [y/N]: y
Superuser created successfully.

python manage.py makemigrations - po dodaniu tabel dla bazy (klas)

python manage.py runserver - uruchamianie serwera

# pododaniu tabel w bazie danych:
python manage.py makemigrations
python manage.py migrate

python manage.py shell - uruchomienie pythona
w konsoli pythona wpisujemy:
    from myapp.models import Post
    Post.objects.create(title="Pierwszy Post", content="To jest mój pierwszy wpis w bazie!")